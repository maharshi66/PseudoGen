<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PseudoGen" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/bright_orange</item>
        <item name="colorPrimaryVariant">@color/bright_orange</item>
        <item name="colorOnPrimary">@color/indigo</item>
        <item name="colorSecondary">@color/light_grey</item>
        <item name="colorSecondaryVariant">@color/dark_grey</item>
        <item name="colorOnSecondary">@color/indigo</item>
        <item name="android:windowBackground">@drawable/background_gradient</item>
        <item name="fontFamily">@font/maven_pro_medium</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="itemTextColor">@color/dark_grey</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="searchViewStyle">@style/MySearchViewStyle</item>
    </style>

    <!-- Action Bar Style -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/bright_orange</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:windowBackground">@drawable/background_gradient</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/bright_orange</item>
    </style>

    <!-- Bottom Navigation Style -->
    <style name="Widget.BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="fontFamily">@font/maven_pro_medium</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- List View Style -->
    <style name="Widget.AppCompat.NewListView" parent="Widget.AppCompat.ListView">
        <item name="android:background">@color/code_editor_dark</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.PseudoGen.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.PseudoGen.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.PseudoGen.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CodeEditorTitleEditText" parent="Theme.PseudoGen">
        <item name="background">@color/dark_grey</item>
    </style>

    <!--Alert Dialog-->
    <style name="CutomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/light_grey</item>
    </style>

    <!--Tool Bar Custom Code Editor-->
    <style name="Toolbar.TitleText" parent="TextAppearance.Widget.AppCompat.Toolbar.Title">
        <item name="android:textSize">14sp</item>
    </style>

    <!--Custom Sign In Buttons-->
    <style name="CustomSignInButtons" parent="Theme.PseudoGen">
        <item name="colorPrimary">@color/light_grey</item>
        <item name="colorPrimaryVariant">@color/indigo</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_grey</item>
        <item name="colorSecondaryVariant">@color/indigo</item>
        <item name="colorOnSecondary">@color/bright_orange</item>
        <item name="fontFamily">@font/maven_pro_medium</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Background for the search query section (e.g. EditText) -->\
        <item name="android:background">@color/code_editor_dark</item>
        <item name="android:textColor">@color/code_editor_white</item>
        <!-- Background for the actions section (e.g. voice, submit) -->
        <item name="submitBackground">@color/bright_orange</item>
    </style>

    <style name="MavenProTextViewStyle" parent="android:Widget.TextView">
        <item name="android:textColor">@color/dark_orange</item>
        <item name="android:fontFamily">@font/maven_pro_medium</item>
    </style>
</resources>